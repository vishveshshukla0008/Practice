# console.log(Math.abs(-10.55)); // Changes Negative Values into +;

# console.log(Math.ceil(10.05)); // if float value change the value into next Integer;

# console.log(Math.floor(10.2)) // if floating point exist then value chnages into lowest Integer

# console.log(Math.round(10.49)); // if .f >= 5 then treated as ceil and <5 then treated as float;

# console.log(Math.trunc(10.9)) // remove floating point and returns the values :

# console.log(Math.pow(2, 5)); // 2 to the power 5 = 32

# console.log(Math.max(1, 2, 3, 4, 5, 6, 7, 8, 9, 9.8)); // returns the max value;

# console.log(Math.min(1, 2, 3, 4, 5, 6, 7, 8, 9)); // returns the min value :

# console.log(Math.random()); // between 0 to 1 random value :

# console.log(Math.sqrt(4)) // returns the sqrt of the value :

# console.log(Math.cbrt(27)) // Returns the value after finding cube root value :

# console.log(10.516565.toFixed(2)); // Fixed the value as near
